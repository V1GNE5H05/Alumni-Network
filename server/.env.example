# ============================================
# ALUMNI NETWORK - ENVIRONMENT CONFIGURATION
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=development
BASE_URL=http://localhost:5000

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Option 1: Local MongoDB (MongoDB Compass)
# Uncomment this line to use local database:
MONGODB_URI=mongodb://localhost:27017/alumni_network

# Option 2: MongoDB Atlas (Cloud Database)
# Uncomment and fill in your Atlas connection string:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/alumni_network?retryWrites=true&w=majority

# Database Name
DB_NAME=alumni_network

# ============================================
# SECURITY CONFIGURATION
# ============================================
# JWT Secret Key (REQUIRED - Change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Session Secret (for future session-based features)
SESSION_SECRET=your-session-secret-key

# ============================================
# EMAIL CONFIGURATION (Gmail)
# ============================================
# Required for password reset and email verification
# 
# How to get Gmail App Password:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification
# 3. Go to App Passwords: https://myaccount.google.com/apppasswords
# 4. Select "Mail" and "Other (Custom name)"
# 5. Copy the 16-character password
#
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-character-app-password

# ============================================
# CLOUDINARY CONFIGURATION (Image Uploads)
# ============================================
# Get your credentials from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ============================================
# RATE LIMITING CONFIGURATION
# ============================================
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=5

# ============================================
# CORS CONFIGURATION (Optional)
# ============================================
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000

# ============================================
# INSTRUCTIONS:
# ============================================
# 1. Copy this file and rename it to `.env`
# 2. Update all configuration values
# 3. For production: Use strong, unique secrets
# 4. Never commit `.env` to git (already in .gitignore)
# 5. Restart server after changing .env values

